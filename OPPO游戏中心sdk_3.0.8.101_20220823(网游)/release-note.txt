------------------2016-04-07--------------------------------
1. 整合原有礼包&活动到福利模块并新增签到有礼功能
2. 精简sdk init相关参数，并将部分参数迁移到AndroidManifest.xml中
3. 优化网游支付流程，在安装支付apk后直接继续原有支付操作，不需要用户重新发起支付过程
4. 退出引导增加后台配置化
5. bug fix
------------------2016-08-19--------------------------------
1.简化sdk init 初始化。
------------------2017-03-09--------------------------------
1.响应工信部要求，增加账号实名制，防沉迷。
------------------2017-05-09--------------------------------
1.常规更新，无接口变动，只需替换jar和so文件。
------------------2017-05-12--------------------------------
内部版本号提升，无接口变动，无需测试。

------------------2017-08-21--------------------------------
资源更新，无接口变动，更新需要替换jar和so文件、以及nearme.apk、opay_version。

注意：如果很久没有更新过sdk，更新时按照接入文档，核对下初始化（init） sdk的方式，如果跟文档不一致，请按照最新文档中的方式初始化sdk。

------------------2017-12-18--------------------------------
版本重构，无接口变动，更新需要替换jar和so文件。

------------------2018-04-25--------------------------------
1、去掉了onResume和onPause接口
2、调整了doReportUserGameInfoData接口，新增了参数，具体参考接入文档。

------------------2018-06-21--------------------------------
1、sdk服务端登录验证接口调整，将原来的http请求改为https，域名及接口名称也有修改，接口参数无变动，只是调整了接口地址。
由"http://i.open.game.oppomobile.com/gameopen/user/fileIdInfo" 改为 "https://iopen.game.oppomobile.com/sdkopen/user/fileIdInfo"。
2、sdk客户端接口无变动，UI更新，只需要替换jar和so文件。

------------------2019-02-27--------------------------------
1、资源、UI更新，无接口变动，只需替换jar和so文件。

------------------2019-09-10--------------------------------
1、兼容android Q;
2、提供了aar接入；
3、去掉了短信权限；
4、新增了receiver action。参考文档或者demo manifest，请务必添加。

------------------2020-03-18--------------------------------
支持防沉迷，无接口变动，更新只需要替换SDK libs和assets下的资源文件。

------------------2020-04-24--------------------------------
无接口变动，更新只需要替换SDK libs和assets下的资源文件。

------------------2020-05-18--------------------------------
解决上个版本低端机器偶现的崩溃问题、无接口变动，更新只需要替换SDK libs和assets下的资源文件。

------------------2020-06-10--------------------------------
版本升级，无接口变动，最新的防沉迷版本。游戏更新时只需要替换SDK libs和assets下的资源文件。

------------------2020-11-25--------------------------------
1、SDK UI界面更新。
2、新增儿童误充值识别接口。
3、游戏更新SDK时，可先将SDK libs和assets下的资源文件更新一下，然后就可以编译了，具体接口以接入文档为准。
4、2.0.8.010版本更新注意事项：

新版本需要引入 androidx的v4包 implementation 'androidx.legacy:legacy-support-v4:1.0.0'，不方便引入的用SDK压缩包中提供的core-1.1.0代替。
编译的时候需要加一下
    compileOptions {
        sourceCompatibility JavaVersion.VERSION_1_8
        targetCompatibility JavaVersion.VERSION_1_8
    }

------------------2021-08-10--------------------------------
1、新增<检查OPPO软件商店是否有更新>接口。
2、SDK初始化申请权限被拒、48小时内将不再重复申请。
3、更新SDK时，要替换libs和assets下的资源文件

------------------2021-11-23--------------------------------
1、修复一些线上的防沉迷bug。接口无变动。
2、更新SDK时，要替换libs和assets下的资源文件。

------------------2022-02-15--------------------------------
1、登录、实名认证流程优化。接口无变动。
2、minSDKVersion变为21。
3、去除了READ_PHONE_STATE、READ_EXTERNAL_STORAGE、WRITE_EXTERNAL_STORAGE权限。
4、更新SDK时，要替换libs和assets下的资源文件。

------------------2022-03-10--------------------------------
1、上线客服自助服务功能。接口无变动。
2、更新SDK时，要替换libs和assets下的资源文件。

------------------2022-08-23--------------------------------
1、修改文件结构，CP无需接入assets下的资源文件
2、如果已经接入了旧版本sdk，升级新版本时需要删除原assets目录下的oppo_game_service_***.so，nearme.apk，opay_version三个文件
3、优化实名认证逻辑。
4、优化登录逻辑。
5、优化支付回调逻辑。
6、按照国家工信部要求修复一部分安全漏洞。